export interface IStatusVariants {
  draft: string
  completed: string
  awaiting_quote: string
  'In-Transit(Air)': string
  not_paid: string
  'not-paid': string
  paid: string
  quote_accepted: string
}
